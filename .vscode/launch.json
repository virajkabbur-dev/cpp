{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Debug (Terminal)",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "terminal": "integrated"
        }
    ]
}